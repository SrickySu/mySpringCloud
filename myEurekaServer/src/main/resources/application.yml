server:
  port: 7001
  
spring:
  application:
    name:  myEurekaServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SrickySu/myConfigCenter.git #GitHub上面的git仓库名字
          username: SrickySu
          password: ICQgithubg0913

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
          